for (int i = 0; i < 10; i++) {
    int x = 0;
    int y = 1;
    switch (x + y * 2 - (1 + 2) * 4) {
        case 0:
            return 1;
        case 1:
            return 2;
        default:
            break;
    }

    while (x == 0) {
        y += 1;
        do {
            printf("x = %s", x++);
        } while (x < 0);
    }

    label:
    goto label;
}